# Dependencies
node_modules/
.pnpm-store/

# Build artifacts
dist/
src-tauri/target/
src-tauri/gen/
out/

# TypeScript incremental
*.tsbuildinfo

# Logs
*.log
stderr.txt

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# Environment
.env
.env.local
.env.*.local

# Memory / local state (don't publish working notes)
MEMORY.md
TODO.md
CLAUDE.md
.claude/
.serena/
/docs/

# Local tooling artifacts (code-intelligence index + browser-automation scratch)
.axon/
.playwright-mcp/

# Ad-hoc dev fixtures — files pasted into public/ so the dev server can serve
# them for in-browser paste tests (e.g. fetch('/JOURNAL.md')). Not a real
# public asset dir — Tauri serves dist/, not public/.
public/
